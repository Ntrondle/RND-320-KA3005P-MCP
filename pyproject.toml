[project]
name = "rnd-320-ka3005p-mcp"
version = "0.1.0"
description = "MCP server for the RND 320-KA3005P bench power supply over SSH-to-serial bridge"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.2.0",
    "paramiko>=3.4.0",
]

[project.scripts]
rnd-320-ka3005p-mcp = "server:mcp.run"